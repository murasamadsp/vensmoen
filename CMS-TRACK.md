# CMS / контент — трек до идеала

Живой список доработок Pages CMS и контента. Отмечай `[x]` сделанное.
Легенда важности: 🔴 важно · 🟡 средне · ⚪ мелочь/полиш.

## 🔴 Авто-перевод (главное — решает 2 боли разом)

- [ ] 🔴 **Авто-перевод изменённых полей.** Боль: правишь поле в одном языке — не знаешь, какие переводить в остальных 5. Решение: GitHub Action на пуш → находит изменённые поля (git diff) → переводит их через Claude API на остальные языки → коммитит обратно → деплоит. Также делает заполнение пустого гида тривиальным: пишешь текст в 1 языке → 5 остальных авто.
  - Нужно от пользователя: `ANTHROPIC_API_KEY` как GitHub Secret (стоимость — копейки на правку).
  - Поведение: изменённый язык = источник; перетирает то же поле в остальных переводом. (Манульные правки в других языках для этого поля затрутся — это и есть смысл.)
  - Встроить как шаг ПЕРЕД build в существующий `deploy.yml` (одна джоба, без гонки/рекурсии).
  - **СТАТУС:** скрипт `scripts/auto-translate.mjs` готов (google активен, claude готов, модель настраиваемая). Запуск: `npm run translate`. Режимы: `--fill-todo`, `--source X --paths ...`, `--base <sha>`. В CI/деплой пока НЕ вшито (фаза теста).
  - [ ] решить: качество google устраивает или переходим на Claude (Sonnet/Opus)?
  - [ ] мелочь: google иногда лепит хвост `" ."` — добавить trim в постобработку.
  - [ ] когда устроит — вшить шаг в `deploy.yml` + добавить `ANTHROPIC_API_KEY` (если Claude).

## Контент (пустые поля / переводы)

- [ ] 🔴 **Guide: 9 тем без текста.** В CMS у «Praktisk guide» 9 заголовков без `body` (Helse, Skole, UDI, Penger, Post, Internett, Transport, Butikker, Fritid) — отсюда пустые поля «Tekst» во всех 6 языках. → написать контент (или временно скрыть body у guide, чтобы не выглядело сломанным).
- [ ] 🟡 **Tigrinya `[TODO]`:** `restavfall.accepted` — «палочки для ушей» и «пустой блистер» не переведены на тигринья (стоит `[TODO] ...`).
- [ ] ⚪ Пройтись по всем языкам глазами на качество перевода новых пунктов (палочки/блистер) — авто-перевода не делал, писал вручную.

## CMS — неявные моменты (UX)

- [ ] 🔴 **Системные строки `ui` (34 шт.)** видны редактору в форме (quizNext, skipToContent…). Помечены «редко меняется», но всё равно мусор. → выяснить, поддерживает ли Pages CMS скрытие полей; если да — спрятать.
- [ ] 🟡 **Поле «Tekst» (body)** — это простая textarea: буллеты `•` и переносы строк надо ставить руками. Неочевидно. → добавить help-текст или включить rich-text/markdown-режим.
- [ ] 🟡 **Неочевидно, что одна «языковая» запись = все страницы сразу** (О центре + Правила + Гид + Сортировка). → подумать над структурой/подсказкой.
- [ ] ⚪ Подписи полей на норвежском. Ок, если редактор читает норвежский; иначе — сменить язык подписей.
- [ ] ⚪ E-mail контактов — без подсказки формата/валидации.

## Полиш

- [ ] ⚪ Флаги-эмодзи в названиях языков для узнаваемости (🇳🇴 Norsk и т.п.).
- [ ] ⚪ Проверить `media`-конфиг в `.pages.yml` — нужен ли вообще (картинок в CMS нет).
- [ ] ⚪ Памятка для бухгалтера на 1 страницу: как зайти и править.

## Сделано

- [x] Pages CMS подключён, `.pages.yml` над существующими i18n JSON, 6 языков.
- [x] Деплой на GitHub Pages, origin форка → GitHub.
- [x] Убран мусор Sveltia, восстановлены mental-строки, вычищен мёртвый `desc`.
- [x] В `restavfall` добавлены палочки для ушей + пустой блистер (6 языков).

---

## Твои наблюдения (впиши сюда, что заметил_а в CMS)

- Показываются пустые формы (= 9 тем гида без текста + ti TODO).
- Нет авто-перевода: правишь N полей в одном языке — не понятно, какие переводить в остальных. → см. секцию «Авто-перевод».
